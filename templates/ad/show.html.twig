{% extends "base.html.twig" %}

{% block title %}
    {{ ad.title }}
{% endblock %}

{% block body %}

<div class="container">
    <div class="title" style="background-image : url( {{ ad.coverImage }})">
        <h1>{{ad.title}}</h1>
        <p>{{ad.introduction}}</p>
            <p class="h2">
            <strong>{{ad.rooms}} chambres</strong> pour <strong>{{ad.price}}&euro;</strong> par nuit
            </p>
        <a href="#" class="btn btn-primary">Réserver !</a>
    </div>
    
</div>

<div class="container">
    <div class="row">
        <div class="col-md-8 mb-3">
            {{ad.content | raw}}

            <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-indicators">
                    {% for image in ad.images %}
                        <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="{{ loop.index0 }}" {%if loop.first %}class="active"{% endif %} aria-current="true" aria-label="Slide 1"></button>
                    {% endfor %}
                    
                </div>
                <div class="carousel-inner">
                    {% for image in ad.images %}
                        <div class="carousel-item {%if loop.first %}active {% endif %}">
                            <img src="{{ image.url }}" class="d-block w-100" alt="...">
                        </div>
                        <div class="carousel-caption d-none d-md-block">
                            <h5>{{ad.title}}</h5>
                            <p>{{image.caption}}</p>
                        </div>
                    {% endfor %}

                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
                </div>
            </div>
        <div class="col">
            <div class="row">
                <div class="col-4">
                    <img src="http://via.placeholder.com/64x64" alt="Avatar de Joseph">
                </div>
                <div class="col">
                    <h3>Joseph Dupont</h3>
                    <span class="badge badge-primary" style="color:black">3 annonces</span>
                </div>
            </div>
            <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Delectus enim nam voluptates beatae voluptate odit quas deserunt quam? Quidem, deserunt?</p>
            <p>Ratione perferendis laudantium ullam repellat laboriosam ducimus autem voluptates nulla accusamus! Suscipit eligendi alias laboriosam, sapiente ullam illo et iusto.</p>
            <p>Autem reprehenderit eveniet reiciendis exercitationem eum cum iure aliquid, alias ducimus nisi laudantium commodi nam modi praesentium ut sapiente quod?</p>
        </div>
    </div>
</div>
{% endblock %}